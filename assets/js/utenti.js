fetch('/api/utenti', {
    .then(function => {

     if (condition) {
        
     }   
        
    )}
    
    })
  .then(res => res.json())
  .then(data => alert(data.message))
  .catch(err => alert("Errore nel salvataggio"));